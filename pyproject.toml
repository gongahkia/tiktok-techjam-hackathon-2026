[build-system]
requires = ["setuptools>=69,<82"]
build-backend = "setuptools.build_meta"

[project]
name = "facetflow"
version = "0.1.0"
description = "Offline deterministic conversational product retrieval for TechJam"
requires-python = ">=3.10,<3.15"
dependencies = []

[tool.setuptools]
packages = ["facetflow", "starter", "evaluator", "tests"]
